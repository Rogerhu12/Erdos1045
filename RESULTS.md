# 数学结论

设 $z_0,\ldots,z_{n-1}\in\mathbb C$，判别式定义为所有有序异标号点对距离之积：
$$
\Delta(z)=\prod_i\prod_{j\ne i}|z_i-z_j|.
$$
因此每个无序距离在乘积中出现两次。直径约束为所有距离不超过 $2$，
周长约束采用凸包的支撑函数积分，退化线段的周长为其长度的两倍。

公开总定理 `Erdos1045.main : Erdos1045.Statement.Claims` 包含五项结论：

1. **直径刻画。** 对奇数 $n\ge 2^{100\,000\,000}$ 以及偶数 $n\ge 2^{10^{120}}$，直径约束下的上确界 $M(n)$ 可达到，
   且极值构型在重新标号和平面保向刚性运动下唯一。对上述奇数 $n$，
   极值构型是正 $n$ 边形，并且
   $$
   M(n)=\frac{n^n}{\cos(\pi/(2n))^{n(n-1)}}.
   $$
   对上述偶数 $n=2m$，极值构型的全部直径边由公开定义的规范图精确描述；
   极值构型具有反射对称，当 $3\mid m$ 时还具有指定的非平凡三重旋转对称。

2. **周长刻画。** 对每个 $n\ge 2^{100\,000\,000}$，周长不超过 $2\pi$ 时，
   上确界 $W(n)$ 可达到，并满足
   $$
   W(n)=n^n\left(\frac{\pi}{n\sin(\pi/n)}\right)^{n(n-1)}.
   $$
   所有极值构型都是正 $n$ 边形，并在重新标号和平面保向刚性运动下唯一。

3. **奇偶归一化极限。** 直径问题满足
   $$
   \lim_{m\to\infty}\frac{M(2m)}{(2m)^{2m}}
   =\frac{3^{9/4}}8\exp\!\left(\frac{\pi^2-2\sqrt3\,\pi}{8}\right),\qquad
   \lim_{m\to\infty}\frac{M(2m+1)}{(2m+1)^{2m+1}}=e^{\pi^2/8}.
   $$

4. **代数证书。** 对每个 $2m\ge 2^{10^{120}}$，偶数阶极值点具有公开的代数证书：
   有理方程组在指定窗口内有唯一非奇异根，根坐标及对应几何数据为代数数，
   并给出实际极值构型；特别地 $M(2m)$ 是代数数。

5. **正 KKT 证书。** 对每个 $2m\ge 2^{10^{120}}$，每个偶数阶极值构型都具有原始点
   坐标上的 KKT 证书：活动边集合精确、约束梯度线性无关、乘子严格为正且唯一，
   并满足驻点方程与互补松弛。

上述直径、周长、代数和正 KKT 结论直接使用已证明的显式阈值，不另列重复的“充分大”版本。
偶数阶直径、代数和正 KKT 结论共用阈值 $2^{10^{120}}$。
构建命令为 `python build.py`，验证命令为 `python verify.py`。
使用 `python verify.py --fresh` 可清理本项目的构建产物、从源码重新构建本项目并执行验证；
固定版本的 Mathlib 缓存仍可复用。
